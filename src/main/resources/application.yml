spring:
  application:
    name: XXSC-BackEnd
  profiles:
    active: dev

mybatis:
  mapper-locations: classpath:/mapper/*.xml
  configuration:
    map-underscore-to-camel-case: true
javafamily:
  notify:
    feishu:
      hook-url: https://open.feishu.cn/open-apis/bot/v2/hook/01f84e24-0f8a-4cfc-83cb-c25275709858
      enabled: true  # 是否开启通知, 用于不同环境下的区分(开发, 测试, 生产), 默认为 true


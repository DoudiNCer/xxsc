server:
  port: 38080
  servlet:
    encoding:
      charset: UTF-8
      force: true

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/xxsc?useUnicode=true&characterEncoding=UTF-8&serverTimezone=GMT%2B8
    username: root
    password: YRSyydssszq@@@020913
  redis:
    host: localhost
    port: 6379
    connect-timeout: 10000

logging:
  level:
    com.sipc.xxsc.mapper: debug

javafamily:
  notify:
    feishu:
      hook-url: https://open.feishu.cn/open-apis/bot/v2/hook/01f84e24-0f8a-4cfc-83cb-c25275709858
      enabled: true  # 是否开启通知, 用于不同环境下的区分(开发, 测试, 生产), 默认为 true